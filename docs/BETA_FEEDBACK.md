# Beta Feedback

Use **Send Feedback** in Creator Mode to export a local feedback JSON file.

Feedback files are saved under:

`project_data/feedback/`

The feedback export includes project title, render stage, beta version, and safe diagnostics. It does not export API keys.
